#include <string.h>
#include <cstdlib>
#include <iostream>
#include <fstream>
using namespace std;

int main() {
	char s[100];
	cin >> s;
	
	int count = 0;
	
	for(int i = 0; i < strlen(s); i++) {
		char c = s[i];
		if('0' <= c && c <= '9') count++;
	}
	
	cout << count << "\n";
	
	return 0;
}





